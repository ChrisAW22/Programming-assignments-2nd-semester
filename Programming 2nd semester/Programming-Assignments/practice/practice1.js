// Variables
let age = 25; // number
let name = "Alice" // string
let isStudent = true; // boolean


// OPerators
// Arithmetic + - * / %
// comparison == === != !== < >
// logical && || !
// assignment = += -=

//decision making

let days = 3;
switch (days) {
    case 1: Monday
    case 2: Tuesday
    case 3: Wednesday
    default:
        Invalid days
}

//loops

for (let i = 1; i <= 5; i++) {
    console(i);
}

let i = 1;
while (i <= 5) {
    console.log(i);
    i++;
}


//Arrays

// add/remove 4 push, pop, shift, unshift  let
// iterate 4 foreach, map, filter, reduce  const



